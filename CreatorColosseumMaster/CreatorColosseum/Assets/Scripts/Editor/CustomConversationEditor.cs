/*using UnityEngine;
using UnityEditor;
using System.Collections;

[CustomEditor(typeof(ConversationScript))]
public class CustomConversationEditor : Editor {

    public override void OnInspectorGUI()
    {
        string text;
        DrawDefaultInspector();
        //base.OnInspectorGUI();

        var cs = (ConversationScript)target;
        cs.conversation = EditorGUILayout.TextArea(text, GUILayout.Height(20));
    }
}*/

//commented out to avoid errors
